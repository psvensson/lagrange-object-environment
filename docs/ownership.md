# Ownership registry

This is the authoritative map of architectural ownership for the Object Environment.

The rule is strict:

> Every major subsystem has one owner. Every interaction between subsystems has one owner.

An owner is one architectural code locus — a module, service, adapter, registry, or lower repository/layer. It is not a claim about a human maintainer.

The owner owns the concern's semantic decisions, state-transition/public contract and primary proof surface. Other components may call, observe or adapt it; they may not independently decide the same rule.

A planned owner reserves responsibility but does not claim implementation exists yet.

## Subsystem owners

| Responsibility | Single owner | Location/status | Notes |
| --- | --- | --- | --- |
| Public package surface | Public API module | `src/index.js` | Selects what this package exposes; does not invent subsystem semantics. |
| Headless environment model bootstrap | Environment model | `src/model.js` | Current bootstrap owner until concrete subsystems are split by pressure. |
| Presentation semantics | `Presentation` | `src/model.js` | Semantic subject + presentation context; never image identity or authority. |
| Command semantics | `Command` | `src/model.js` | Command description/applicability; authorization remains below the environment. |
| Presentation discovery | `PresentationRegistry` (`src/presentation-registry.js`) | current | Sole owner of subject/context -> candidate Presentations discovery (synchronous, registration-ordered, no priority, renderer-independent). Discovers; the `Presentation` remains the semantic result. Never renders, never consults authority. Language personalities contribute providers via `PersonalityExtensionRegistry`; selection of one-versus-all is the consumer's/Compositor's concern, not this registry's. |
| Command discovery | `CommandRegistry` (`src/command-registry.js`) | current | Sole owner of subject/context -> applicable Commands discovery (synchronous, registration-ordered, applicability-only). Discovery never filters on authority; a returned Command confers none (ADR 0004). Invocation/authorization belongs to `CommandDispatcher`, not this registry. |
| Generic object experience (inspect/navigate/unavailable/unauthorized) | `ObjectNavigator` (`src/object-navigator.js`) | current | Sole owner of the object -> inspector -> discover refs -> follow ref -> presentation loop (and its unavailable-ref / unauthorized-ref branches). Composes `PresentationRegistry`/`CommandRegistry`/`ImageClientAdapter` — does not re-implement their selection or invocation. Reads the object fresh under EXPLICIT per-call authority via the adapter's authorized read seam (a ref is never authority; authority is threaded, never stored); materializes the outcome into the subject (denied read -> `unauthorized-ref`, missing/backend -> `unavailable-ref`, distinctly), then discovers synchronously. Never renders. Generic providers (`src/object-presentation-providers.js`) plug into the registry rather than hard-coding selection. |
| Durable Project browsing (read-only) | `ProjectBrowser` (`src/project-browser.js`) | current (Bead mky) | Sole owner of Project-specific orchestration from Project subject -> authorized canonical ProjectDescriptor -> exact-one Project Presentation -> `project-view` browse/open/refresh/follow requests; `Compositor` remains the sole owner of view admission and logical lifecycle. It consumes `ImageClientAdapter.readProject` and `PresentationRegistry`, selects exactly one matching `project` Presentation, preserves the Images-owned descriptor by identity (no membership/target copy or normalization), and serializes presentation changes. Replacement open is fail-closed: it stops prior follows, requests closure of the prior Project view, and admits a new active subject only after a successful read/discovery/view open; generation cancellation releases serialized work from uncancellable stale reads. Observation carries metadata only and triggers a fresh Project read/re-presentation; no backing Project id/slot/member record is known here. This owner also resolves a transient current-descriptor integer action key to that canonical member's target ref; the durable member key remains member identity, and target authorization/navigation stay with `ObjectNavigator`/`ImageClientAdapter`. No Project mutation Commands, renderer semantics or authority issuance. |
| Perspective semantics | `Perspective` | `src/model.js` | Durable environment intention; image persistence is reached through the image interaction owner. Durable representation is defined by ADR 0012 (superseding ADR 0008's representation). |
| Session semantics | `Session` | `src/model.js` | Ephemeral client interaction state. |
| Renderer-specific graphics and host resources | `RendererAdapter` | contract defined (`src/compositor.js` boundary); `BrowserRendererAdapter` realization current (`src/browser-renderer/browser-renderer-adapter.js`) | Owns ALL concrete renderer resources (browser/native GPU, device, queue, surface, frame, Component/WIT graphics host providers). Returns only OPAQUE, TRANSIENT, Session-scoped string handles upward; never exposes DOM/WebGPU/GPU objects or raw GPU ops across the boundary. The Compositor-facing contract is lifecycle-only and data-representable (remote-friendly); does not own semantic Presentation or composition policy. |
| Renderer render-target realization | `BrowserRendererAdapter` host wiring (`src/browser-renderer/render-target.js`) | current | Sole owner of WHERE a surface-webgpu "current texture" comes from: `CanvasRenderTarget` (on-screen `<canvas>`, browser presentation) or `TextureRenderTarget` (host-owned `GPUTexture`, headless/test/export, deterministic read-back). The renderer Component, the Presentation, and the Compositor are all unaware which realization exists — the choice is host wiring (`createRenderTarget` / `pushPendingRenderTarget`). This is the seam a future remote renderer plugs into; it never crosses the Compositor boundary. |
| Renderer asset transfer (durable bytes -> Component) | `BrowserRendererAdapter` host wiring (`src/browser-renderer/assets.js`) | current | Sole owner of the runtime asset-transfer seam (`lagrange:assets/provider@0.1.0`): durable asset bytes cross the host -> Component boundary per-attach, under authority, scoped per-instance. The Component is transpiled in jco INSTANTIATION mode (`instantiate(getCoreModule, imports)`), so per attach the adapter builds a fresh provider via `createAssetProvider(allowlist)` and binds it as `imports['lagrange-assets']` for THAT ONE Component instance. There is NO module-global provider/registry/"current attach"/ambient byte store — Component A's `load` closure contains only A's allowlist, so it cannot name-or-guess Component B's bytes (proven: A renders its own bytes, unaffected by B's instantiation). The provider closure is dropped with the instance (teardown). The Component calls `load(name)` with a PRESENTATION-LOCAL name; it never learns the underlying image/object identity (reference != authority). The host treats bytes as opaque (format-neutral `load`, not `load-glb`). The asset-bytes AUTHORITY (resolving durable refs -> bytes under `object/read`) is the environment's authorized read lane — see the "Presentation asset refs -> authorized bytes" interaction-owner row. |
| Durable image/object/project semantics | Lagrange Images | external: `psvensson/lagrange-images` | This repository is a public consumer; no shadow object/project/history store here. |
| Image authorization enforcement | Lagrange Images execution boundary | external: `psvensson/lagrange-images` | Authority is transient execution context; references/presentations/commands confer none. |
| Root authentication/principal and authority issuance | Lagrange/control-plane identity/authority layer | external/planned integration | The environment consumes normalized identity/authority operations; it does not implement passwords/OIDC/Keycloak policy. |
| Agent task/dependency graph | Beads | `.beads`/Dolt after `bd init` | Authoritative operational work tracker; no duplicate Markdown TODO tracker. |
| Short durable agent discoveries | Beads memory | `bd remember` | Architectural discoveries must also be promoted to tests/docs/ADR when they change project truth. |
| Architecture decision history | ADR set | `docs/decisions/` | Why durable decisions were made. |
| Current ownership map | Ownership registry | this file | Must change whenever a major owner/boundary changes. |
| Renderer Component build tooling + artifact byte identity | `renderer-component/build.sh` (+ `renderer-component/rust-toolchain.toml`, `Cargo.toml`/`Cargo.lock`) | current (Bead ocj) | Sole owner of HOW the checked-in GLB Component artifacts under `test/browser/components/glb/` are produced and of the artifact's byte identity: the exact toolchain (rustc 1.89.0 via the nested toolchain file, wasm-tools 1.244.0, jco 1.32.1) and the `--locked` dependency set, and the executable identity gate (`EXPECTED_COMPONENT_SHA256`, deliberate re-pin only via `ALLOW_COMPONENT_SHA_CHANGE=1`). The `hosts/linux` sha256 pins (`tests/l1_portability.rs`, `src/main.rs`) CONSUME that identity and are the only executable assertion of ADR 0013's same-bytes claim today; the browser proofs load the artifact but pin no sha (Bead iwu). Neither co-owns the identity. CI never builds the Component; regeneration is a manual, gated, reviewed act. |

## Interaction owners

The endpoint owners do not jointly own an interaction. Each boundary has one owner responsible for translation/protocol, ordering, error semantics, lifecycle and integration proof appropriate to that boundary.

| Interaction | Single interaction owner | Status | Responsibility |
| --- | --- | --- | --- |
| External caller -> Object Environment package | Public API module | current: `src/index.js` | Defines the package-facing entry surface and composition points. |
| Object Environment -> Lagrange Images | `ImageClientAdapter` (`src/image-client-adapter.js`) | current | Sole environment-side adapter for public image APIs, observation, authorized operations, durable Project reads and durable Perspective projection. Lagrange Images still owns its own public semantics. Composes the cores: observation `src/image-observation.js` (ADR 0009, as amended by substrate ADR 0070 — authorized metadata-only invalidation feed), command dispatch `src/command-dispatcher.js` (ADR 0010), Perspective projection `src/perspective-projection.js` (ADR 0012). The leaf/edge object loop, authorized ProjectDescriptor read, and Perspective save/load round trip are proven against a real runtime. Project reads call Images' public `authorizedReadProjectDescriptor` and bridge only its owner-created demand to the injected runtime authority service as `authority.require(callerContext, demand)`; the adapter never constructs/inspects a Project demand, knows no Project storage IDs/slots, and returns the canonical descriptor unchanged. Images owns authorization-before-existence, the one-Project-read-covers-backing-members rule, storage translation and canonicalization; member targets remain independent refs requiring their own object/read. Save is a staged authorized workflow: the adapter obtains a fresh opaque authority context per image invocation from an injected authority provider (it neither issues nor inspects contexts), creating children first and the indexed Perspective last (the commit point). Ordinary object reads use the authorized whole-record object/read lane (`image-object-read-binding/v1`, substrate ADR 0068): `readObject`/`authorizedReadObject` are the environment's SINGLE user-facing generic-object read seam, crossing `object/read` under explicit authority; the privileged `images.getObject` survives only for control-plane/schema reads (trusted host) and mutateObject's version-token fetch, never on a user-facing path. Load reads the Perspective and EACH child as a separate authorized read via an injected read authorityProvider (ref != authority). |
| Perspective -> durable image representation | `ImageClientAdapter` (`src/perspective-projection.js`) | current: projection core + adapter save/load (ADR 0012, formatVersion 3) | Owns the Perspective <-> ordinary-image-object encode/decode contract of ADR 0012 (superseding ADR 0008). `Perspective` owns semantics; Lagrange Images owns storage. Implemented: a Perspective object holding its presentation refs in its indexed part (durable child MEMBERSHIP + canonical record ENUMERATION order — NOT arrangement/layout order, which the composition tree owns separately) plus one child object per presentation; membership/enumeration have a single owner (no ordinal, no back-edge). The `layout` slot is OPAQUE ref-free JSON to this projection (slot mechanics only); the COMPOSITION meaning of that payload is owned by `src/composition-persistence.js`. Load enumerates the indexed part via object/read-level access. |
| Composition tree <-> Perspective layout slot | `src/composition-persistence.js` | current | Sole owner of the tree<->Perspective boundary: (1) the tree<->presentations BIJECTION (`presentation.id === durable viewId`; `set(leaf viewIds) === set(presentation ids)`, unique both sides; orphan/dangling/duplicate reject) ENFORCED at the save/restore seam; (2) the versioned composition-layout codec (`layout = {kind:'composition', version:1, root:<tree>}` — a separate version axis from the Perspective graph formatVersion, which stays 3; unknown composition versions reject loudly; a LEGACY layout with no `kind:'composition'` is preserved opaquely and yields no tree, never coerced or rejected); (3) the `empty()` zero-case mapping. CONSUMES the composition kernel (`validate`/`leafViewIds` — the kernel stays the single owner of a well-formed node); CONSUMED BY the Perspective projection at the layout-slot boundary (the projection stays opaque). Never persists a `viewDescriptor` (renderer/surface realization is not durable arrangement intent). Also owns the restore-orchestration seam (`restoreComposition`): decode the tree, re-check the bijection, then `openView` each leaf with its SAME durable viewId + a presentationDescriptor projected from the Presentation (`parameters` <- `context`; `state` does NOT reach openView) + a CONCRETE viewDescriptor supplied by an injected world/surface realization POLICY (never from the Perspective). |
| Presentation -> renderer | `RendererAdapter` | contract defined; realizations planned | Converts semantic presentation output into renderer-specific view operations without moving semantics into the renderer; for Component-backed presentations, supplies the exact-version graphics/surface host interfaces the renderer declares (on the renderer-Component -> graphics-host side, BELOW the Compositor boundary). |
| Compositor -> concrete render surface | `RendererAdapter` | contract defined (`src/compositor.js`); realizations planned | Owns translation from logical surface/view lifecycle requests (createSurface/attachPresentation/detachPresentation/resize/destroySurface/destroyAll) into concrete renderer surfaces and GPU resources, returning only opaque transient handles. `Compositor` owns logical arrangement and lifetime intent; `RendererAdapter` owns host-resource realization. This is the remote-friendly boundary (one level above wasi:webgpu): all args are data-representable, so a future RemoteRendererAdapter can move the renderer across a network without changing Compositor/Presentation/Perspective semantics. |
| Input/gesture/key/menu -> semantic command | `CommandRouter` (`src/command-router.js`) | current | Resolves UI invocation policy to one command + semantic subject; does not authorize the command. Consumes a semantic INTENT DESCRIPTOR (plain data, e.g. `{kind:'activate'}`, never a pixel coordinate, never a subject) + a renderer surface handle; resolves the subject from the Compositor's durable intent (`viewForSurfaceHandle` -> `presentationDescriptor.subject`, ADR 0011 §6); discovers (applicability, never authorization) via `CommandRegistry`; dispatches via the ordinary authorized `dispatch` seam with a PER-INVOCATION authority context from an injected `authorityProvider` (never minted/stored). The renderer only says 'an interaction happened on this view'. |
| Renderer input capture + synthetic injection | `Surface` + `BrowserRendererAdapter` host wiring (`src/browser-renderer/surface.js`, `browser-renderer-adapter.js`) | current | Sole owner of pointer input streams and the semantic-intent resolution. Real pointer events flow from DOM listeners (canvas realization, when the canvas is mounted) into a per-Surface stream; the host synthetic-injection seam (`injectPointerEvent`) pushes into the SAME stream, so the unmounted/headless CI path drives the identical downstream path. The adapter resolves a pointer interaction to a semantic intent descriptor (via `onIntent`) — it emits ONLY 'an interaction happened on this view', never a subject. Below the Compositor boundary; `injectPointerEvent`/`onIntent` are host-inspection seams, not part of the lifecycle-only `RENDERER_ADAPTER_METHODS`. |
| Command -> image operation | `CommandDispatcher` (`src/command-dispatcher.js`) | current | Owns command invocation sequencing/result/error mapping and the typed error taxonomy of ADR 0010; passes authority through opaquely (never holds it) and delegates the image crossing to `ImageClientAdapter` (`src/image-client-adapter.js`), which invokes the command through its seam. |
| Presentation asset refs -> authorized bytes | `ImageClientAdapter` (`src/image-client-adapter.js`) authorized read lane | current (`resolveAssetBytes`) | Sole owner of resolving a presentation's PRESENTATION-LOCAL asset names (mapped to durable refs by the Presentation) to opaque bytes under explicit per-attach `object/read` authority. This is a read of a durable object under authority, so it lives in the SAME authorized read lane as `readObject`/`authorizedReadObject` (no separate grant type, no broad asset grant). Reading a Presentation does NOT transitively authorize its assets — each asset ref needs its OWN authorized resolution (ref != authority). The `BrowserRendererAdapter` receives only the resolved `Map<presentationLocalName, Uint8Array>` (never refs/ids/authority) and stays a byte conduit; it never caches authority (bytes are held ephemerally per-attach, authority is never stored). |
| Composition tree (arrangement-structure intent) | `src/composition-tree.js` | current | Sole owner of the renderer-independent ARRANGEMENT-STRUCTURE tree: `empty()` / `presentation(viewId)` / `split(axis, ratio, first, second)` / `stack(children, active)`. Immutable, data-representable, validated (fails loudly on malformed nodes). Leaves carry ONLY a durable logical `viewId` (data, never a live Presentation instance, renderer handle, or surface); internal split/stack nodes carry NO identity. Holds NO focus, NO semantic selection, NO authority, NO DOM/geometry, and NO window/tab/notebook/spatial policy (a later world/surface-policy concern). `split` is relative layout (a ratio, not pixels); `stack.active` is the durable viewId of the exposed child — strictly distinct from transient focus (the Compositor may initialize focus from a restored active child but never writes focus back into the tree). The Compositor CONSUMES/realizes this tree (opens/arranges the logical views it names); the tree itself never touches the renderer. |
| Session -> composition/render lifecycle | `Compositor` (`src/compositor.js`) | current | Owns ephemeral logical view lifecycle: which presentation is shown, its logical view lifetime, and teardown on Session destroy. Also the SOLE owner of **viewId allocation/admission** (mint fresh, or re-admit a previously-issued durable ID on restore — validated + uniqueness-enforced; the persisted ID IS the Session ID, no remap). Holds surface handles ONLY as transient Session-scoped state in its private view map (never in Session.state, a Perspective or the image); a Perspective is rebuilt from durable intent ({viewId, viewDescriptor, presentationDescriptor}) only, handle-free by construction. Requests realization from `RendererAdapter` across the data-representable boundary; does not implement GPU/WIT. Also owns **focus** (which logical view receives interaction — transient Session state, about a VIEW, never serialized to a Perspective). ARRANGEMENT STRUCTURE (the composition tree) is owned separately (`src/composition-tree.js`); the Compositor realizes it. World/surface policy (tiling/windows/notebook/spatial) is a later slice. |
| Semantic selection | `SelectionModel` (`src/selection-model.js`) | current | Sole owner of SEMANTIC SELECTION — which semantic subject(s) the user means (semantic identity, about OBJECTS). Strictly distinct from focus (a Compositor concern, about a VIEW). Holds a semantic subject keyed by identity (imageId+objectId), NEVER a renderer surface handle — renderer teardown/recreation cannot change it. Confers ZERO authority (identity, not a capability). Transient by default; never written to a Perspective (only a later explicit promotion may). The subject comes from the environment's own view/presentation structure (the caller supplies it from a view's presentationDescriptor), never from renderer input. |
| Interaction -> focus + selection | `Compositor.interactWithSurface` (`src/compositor.js`) | current | Sole owner of the renderer-interaction -> focus/selection seam: given a surface handle ('an interaction happened on this view'), focuses the bound logical view AND updates the SelectionModel with the view's semantic subject (from presentationDescriptor, never the renderer). Keeps focus (Compositor) and selection (SelectionModel) in step WITHOUT conflating them into one UiState. |
| Selection -> inspector presentation | `EnvironmentShell` (`src/environment-shell.js`) | current | Sole owner of the 'selection changed -> update the inspector view's Presentation' coupling (nothing else owns it: SelectionModel owns selection state, Compositor owns views). A HEADLESS, renderer-independent orchestration core consuming ObjectNavigator/SelectionModel/Compositor via public APIs; NEVER reads the image directly (every read through `ObjectNavigator.navigate`, which owns the unauthorized/unavailable materialization); NEVER owns authority (threaded per-call); NEVER owns discovery (the registries), the composition tree, focus, or rendering. Ref-row activation is a SELECTION gesture (`selectionModel.select(ref)` + `focusView(navigator-view)`), NOT a CommandRouter dispatch. Owns the observation -> reread subscription LIFECYCLE (`followSelected`): one observe iterator filtered to the selected identity; on a metadata-only invalidation, re-navigate the selected ref under per-call authority and `presentOn` the inspector — NO shadow UI-side object state (an invalidation triggers a fresh authorized reread). The first useful composed environment (navigator + inspector in a split). |
| Renderer presentation realization dispatch | `BrowserRendererAdapter` host wiring (`src/browser-renderer/browser-renderer-adapter.js` + `component-realizer.js` + `dom-realizer.js`) | current | Sole owner of the kind->realizer DISPATCH seam (an injected `realizerFor(descriptor) -> Realizer` resolver, NOT a hard-coded kind switch and NOT a registry object). The adapter stays the LIFECYCLE owner (opaque handles; the 6-op contract unchanged); each handle holds an opaque `Realization {start, stop, resize, readPixels, dispose}`. The Component/WebGPU realizer (3D kinds) and the DOM realizer (tool kinds: navigator/inspector/project/unavailable/unauthorized — native semantic HTML, DOM never crosses the boundary) are SIBLING realizations. DOM interactions emit DESCRIPTOR-LOCAL ITEM KEYS (`{kind:'activate-item', key}`), never semantic refs/subjects. |
| Renderer activate-item -> selection | `EnvironmentShell` (`src/environment-shell.js` `handleActivateItem`/`bindIntents`) | current | Sole owner of resolving a renderer-supplied DESCRIPTOR-LOCAL item key to a semantic ref for selection. An activation binding names exactly one surface, source view and pure descriptor-local resolver. On `{kind:'activate-item', key}`, the shell passes the named view's CURRENT `presentationDescriptor` + key to that resolver, then sends its returned ref unchanged through `selectObject`/`ObjectNavigator` and focuses the source view. Navigator reference indexing is the backward-compatible default; Project member indexing remains owned by `ProjectBrowser` and is only injected here. A stale/out-of-range key resolves to null and does not select. The key is meaningless without the current descriptor. NOT CommandRouter (which resolves a view subject, not a descriptor-local row; selection is not a Command dispatch). The EDIT-field twin of this table is row 64 (edit bindings). Known gaps: bindings capture surface handles at bind time (Bead 4o8); `handleActivateItem` still resolves the descriptor from `durableIntent()`, which includes lost views (Bead 8ik); the table has no non-unit consumer yet (Bead 3hn). |
| Renderer edit-field intent -> edit binding (field resolution + transient token) -> CommandRouter (dispatch is NOT here) | `EnvironmentShell` (`src/environment-shell.js` `bindIntents` edit bindings / `handleEditIntent`; `handleEditField` is the inspector's instance) | current (Bead 6lm generalized Bead tow's inspector-only path) | Sole owner of routing a renderer-supplied `{kind:'edit-field', key, text}` to a semantic edit operation via ONE table of EDIT BINDINGS `{surfaceHandle, viewId, resolveField, tokenFor, commandId, onEdited, onEditError}` (mirrors the activate-item table, row 63; an activation binding and an edit binding MAY share a surface, surfaces are unique WITHIN each kind; every binding MUST declare its own `commandId` — none inherits the inspector default, which would let the router fall back to an arbitrary applicable Command; the inspector view can NEVER be bound through `editBindings`). On an intent the shell resolves the LIVE view for the surface via `Compositor.viewForSurfaceHandle` (the same source `CommandRouter.consumeIntent` uses for the subject, so descriptor and subject can never belong to different views; `viewId` is an assertion, mismatch = no-op), asks the binding's PURE consumer-owned `resolveField(descriptor, key)` for the descriptor-local field context (null = stale/unknown key = explicit no-op, never a wrong field; a plain data-only object incl. `{}` = dispatch — plainness (prototype `Object.prototype` or null, same realm) is the shell's own check and the JSON round trip is the Compositor's exported `assertDataRepresentable`; anything else incl. `undefined`, strings, arrays, class instances = error, no dispatch, reported ONCE via the binding's `onEditError` when present, else propagated to a direct caller / swallowed by `bindIntents`' fire-and-forget path — the pre-6lm double report from the outer catch is gone), asks `tokenFor(descriptor)` for the consumer's transient version token (the shell stores/interprets NO token for foreign views — concurrency semantics stay with the view's owner), and routes through `CommandRouter.consumeIntent({kind:'edit-field', key}, {surfaceHandle, context: {commandId, text, versionToken, field}})` with the resolver result NESTED under `field` so it can never collide with or re-target the shell's own keys. The shell builds NO Command and dispatches NOTHING itself (CommandRouter RETAINS semantic-intent -> Command/authority/dispatch ownership; `onEdited` receives the router result verbatim, `null` = not routed); the RAW text passes through unparsed. The INSPECTOR is one binding constructed internally: resolver = key -> writable slot over the CURRENT inspector descriptor (`field: {slot}`), `tokenFor` = the shell's paired inspector token (TRANSIENT concurrency state paired with each successful read->presentOn; replaced on reread; cleared on subject change/unreadable — NEVER in Presentation/SemanticUi/presentationDescriptor/durableIntent/Perspective), plus two INTERNAL hooks no public binding can set: the olm edit barrier (which guards the shell's OWN `followSelected` reread ordering; foreign bindings take no barrier — their follow/edit races belong to their owners, e.g. Bead 7c8) and the inspector reread offered on error. Nothing view-specific beyond that: no field semantics, no storage representation, no assumption about what a Command does with `field`. A binding is only as live as its surface handle (a re-opened view mints a fresh handle; Bead 4o8). Proofs: `test/environment-shell.test.js` (edit bindings: routing by (kind, surface), nested/fenced context, live-view resolution, null vs `{}` vs malformed, no barrier and no inspector side effects for foreign edits, bind-time validation), `test/environment-shell.integration.test.js` (inspector path against the real runtime). No SemanticUi producer emits `edit-field` off the inspector surface yet (Bead ndk): the generic path is proved at the intent contract. |
| Native Linux Component host (L1 portability proof) | `GlbHost` + offscreen `surface-webgpu` (`hosts/linux/src/lib.rs`) | current (falsification spike, ADR 0013) | Sole owner of running the EXACT checked-in GLB renderer Component core binary (`test/browser/components/glb/glb.component.wasm`, sha256-pinned == the browser-tested artifact) under Wasmtime with native `wasi:webgpu` + `wasi-gfx:surface` + `lagrange:assets/provider` — NO jco/JS/Node/browser/DOM/winit. A native Component host/proof, NOT a `LinuxRendererAdapter` (the six-op adapter portability falsification is L3). Headless offscreen (native analogue of the browser `TextureRenderTarget`): a custom `OffscreenContext` hands the Component a host-owned per-frame texture on its own device and `present()` reads it back. Per-instance isolation = one Wasmtime `Store` per instance. Findings (recorded): a readback command buffer must be `queue_submit`ted (map does not run pending work); queue identity via the public `HostGpuDevice::queue` host method; Wasmtime `Linker::instance()` is define-only (no method shadow/interception). The on-screen native-window proof is a separate follow-up Bead. |
| SemanticUi contract (SemanticUi/v1) + projector | `src/semantic-ui.js` | current | Sole owner of ADR 0013's host-neutral tool-UI description: the vocabulary (group/text/field/collection/action(key)), the `semanticUiForPresentation` projector (owns ALL semantic shaping that used to live in the DOM realizer — heading text, unavailable-vs-unauthorized reason line, field normalization, reference rows -> action(key), and Project name/id/namespace/member display), the Value->display-text normalization, and the validation. Project member actions display the Images-owned stable member key, role and cross-Image target as text, but carry only a transient current-descriptor integer key—never a ref/target/subject. A SemanticUi document is TRANSIENT realization data (display strings + descriptor-local action keys ONLY — no refs/subjects/authority/DOM/GTK/geometry/Commands). Validation LOUDLY rejects host-specific fields, refs-in-actions, unknown versions/kinds. EDITING (minimal): a `field` may carry OPTIONAL `{key, editable:'text'}` — ONLY together, ONLY when the slot is in the host-neutral `writable` set (single owner: `ImageClientAdapter.writableSlots`, threaded as `parameters.writable`). The projector derives editability from `parameters.writable` and assigns each editable field a descriptor-local key. The edit intent is RAW-STRING `{kind:'edit-field', key, text}` (text is the ONLY editable scalar; NO numeric/bool parser — raw text has one host-neutral interpretation). The key domain is non-negative SAFE integers (both validators) so keys survive the cross-host round-trip identically. The checked-in fixture corpus (`test/fixtures/semantic-ui/`, green + red) is the conformance suite BOTH the JS and the Rust validators run against (one authoritative contract, two conforming validators). Host-specific realizers (browser DOM `src/browser-renderer/dom-realizer.js`, Linux GTK) CONSUME the description; they never re-decide semantics. The projector is called INSIDE a realizer's attach path (the adapter/Compositor boundary is unchanged; SemanticUi never enters `durableIntent`). |
| SemanticUi -> native (GTK) realization | `hosts/linux/src/semantic_gtk.rs` + `semantic_ui.rs` | current | The LINUX realization of SemanticUi/v1 (ADR 0013's native tool-UI route). `semantic_ui.rs` is the Rust conforming validator/deserializer (mirrors the JS validator's tolerance exactly — see the contract's CONFORMANCE NOTE); `semantic_gtk.rs` builds REAL GTK4 controls from a validated document (group->section, text->label, field->row, collection->button list, action(key)->a real `gtk4::Button`) and emits the SAME plain-data intent `{kind:'activate-item', key}` as the browser (descriptor-local, never a ref). Consumes the SAME checked-in fixtures the browser consumes (cross-host identity). GTK APIs stop at this boundary — nothing above learns what a `gtk4::Widget` is. Headless: `gtk4::init()` under Xvfb; programmatic `emit_clicked` activation; GTK is main-thread-only (widget tests are a single `#[test]`). |
| Native Linux RendererAdapter (L3 coexistence) | `LinuxRendererAdapter` (`hosts/linux/src/linux_adapter.rs`) | current (falsification proof, ADR 0013) | Sole owner of the SIX-OP RendererAdapter contract on native Linux — a SIBLING of `BrowserRendererAdapter`. Implements EXACTLY `createSurface`/`attachPresentation`/`detachPresentation`/`resize`/`destroySurface`/`destroyAll` (the `RendererAdapterOps` trait is the compile-time no-widening falsification) over ONE opaque-handle surface map. Owns native kind->realizer dispatch: tool kinds -> the L2 GTK realizer (via the Rust projector), `glb` -> the L1 `GlbHost` offscreen path (a live `GlbInstance` held open across frames). Re-implements the data-representable loud-reject (`assert_data_representable`; the JS Compositor is not in this loop). Handles are opaque, transient, process-unique strings; a GTK widget tree / Wasmtime instance NEVER crosses as identity. `read_pixels`/`pump_frames`/`activate_gtk_action`/`gtk_*` are HOST-SIDE inspection seams (NOT contract ops), mirroring the browser's `readRenderedPixels`/`onIntent`. THREAD MODEL: all GTK on the owning thread (main-thread-only); all GLB async on an injected tokio-runtime bridge (`glb_runner` = `block_on`); the six ops are the only crossing. Proven by `tests/l3_coexistence.rs` (ONE `#[test]`: composition {navigator\|inspector\|glb}, a GTK navigator action -> key->ref -> presentOn the inspector while GLB stays live, destroy/recreate from the same durable intent -> FRESH realizations). The native fixture replicates the Compositor/EnvironmentShell CALL SEQUENCE; it does NOT run the JS core. On-screen window remains Bead 64j. |
| Language personality -> environment presentations/commands | `PersonalityExtensionRegistry` | planned | The language-personality contribution boundary: a personality contributes presentation providers and commands, which it feeds into `PresentationRegistry`/`CommandRegistry`. It is NOT the generic discovery owner — it does not answer `discover(subject, context)`; the two generic registries do. Keeps each language extending one environment rather than creating another IDE architecture. |
| Beads task/memory -> agent work session | `bd prime` workflow | current/tool-owned | Operational context injection and work discovery; project-specific governance remains in `AGENTS.md`. |
| Environment JS <-> embedded JS runtime host (3zb-A fallback) | `hosts/linux/src/js_env/` (embedded-JS-runtime host port) | current (bounded rquickjs fallback plus fake and artifact-backed real full-loop acceptances landed — Bead 3zb, ADR 0014 fallback) | **`js_env` owns the standard JavaScript host-runtime facilities the embedded guest requires — currently AbortController/AbortSignal, timers, TextEncoder/TextDecoder and structuredClone, plus the generic crypto primitives — and owns their HOST REALIZATION ONLY. It does not own Lagrange Images semantics, crypto policy, persistence, authority, tokens, or object meaning.** Concretely: sole owner of hosting the EXISTING checked-in Environment ESM core (`src/*.js`, unchanged — no native source fork) inside a native rquickjs runtime in the Linux client, and of the TWO plain-data capability ports that cross it: the RENDERER port (the six-op `RendererAdapterOps` contract -> the existing `LinuxRendererAdapter`) and the IMAGES port (a plain-data async capability shaped as the eventual WIT boundary). INVARIANT: NO semantic logic in Rust — the host owns ONLY the guest lifecycle (one explicit JS-runtime execution owner; the pump/re-entry sequencing; the embedded-source ESM loader mapping known module names to checked-in source bytes, never CWD-relative in production), the minimal host-global shims the census proved the Env closure needs (AbortController/AbortSignal with listener semantics, setTimeout/clearTimeout via the owner pump; Promise/queueMicrotask/atob/btoa are QuickJS-native), and — added by Bead 3zb slice B1a — the WEB-STANDARD UTF-8 coders `TextEncoder`/`TextDecoder`, which are NOT Node compatibility and NOT an engine feature (both are `undefined` in the pinned QuickJS-NG, as is `crypto`): the real lagrange-images portable closure needs them, `src/support/portable-bytes.js` is its only coder user, and `composite-codec.js` calls `utf8Encode('LGIC')` at MODULE TOP LEVEL, so they must exist BEFORE module evaluation. They are WHATWG-faithful because two Images behaviours depend on it: `smalltalk-primitives-bytes.js`'s `decodeUtf8Strict` validates UTF-8 by a decode/re-encode ROUND TRIP (a decoder that maps `ED A0 80` back to a lone surrogate would let malformed bytes validate), and `utf8Encode` feeds `sha256`/`base64urlEncode` on the type-fingerprint/version-token paths (an encoder emitting WTF-8 instead of `EF BF BD` would produce a different durable identity than the Node reference). The encoder is pure JS and the decoder is Rust-backed — an asymmetry FORCED by the engine, not chosen: a JS string holding a lone surrogate cannot cross into Rust (`JS_ToCStringLen2` keeps it as WTF-8 and `str::from_utf8` then fails), so the exact input a faithful encoder must map to U+FFFD is the one that cannot reach Rust. Unsupported options (`fatal:true` beyond the implemented strict decode, `stream`, `ignoreBOM`, `encodeInto`, non-UTF-8 labels) FAIL LOUDLY rather than degrading. Bead 3zb slice B1c adds `structuredClone` on the same terms — web-standard, absent from the pinned engine, and required by the REAL Images closure rather than by any Environment module (3zb-A correctly excluded it: the census's hits were all `lagrange-images`, BELOW the capability port; B1b is the first slice to run that closure IN-PROCESS, which turns the need into a host obligation). It is the STANDARD global, not an Images-specific deep clone: identity is preserved (shared references stay shared, cycles stay cycles, views over one `ArrayBuffer` still share one cloned buffer), and functions/symbols/exotic prototypes plus `{transfer:[…]}` are refused with a DataCloneError-shaped exception rather than silently degrading to a shallow or lossy copy. Bead 3zb slice B1b adds five GENERIC synchronous crypto PRIMITIVES as host globals (secure random bytes, SHA-256, AES-256-GCM encrypt/decrypt, UUID v4; RustCrypto `aes-gcm`/`sha2` and OS `getrandom`, never hand-rolled). They are PRIMITIVES ONLY: this owner assembles no provider object, performs no contract validation (`assertCryptoProvider` is Images-owned and must not be reproduced), and names no Images concept — no cursor, version-token, type-fingerprint, derivation, authority or Project semantics, enforced by file-scoped needles in `host_runtime_structural_guard.rs` plus required-needle checks so a fence cannot pass by deletion. The `NO node:crypto` rule above still holds and is now needle-guarded, as is the absence of a WebCrypto personality: crypto reaches Images through its own provider contract, never a `crypto` global., and plain-data marshalling across the ports. NO Buffer/node:crypto/fs/process/child_process shim, NO Node module personality, NO lagrange-images imports, NO Node subprocess bridge or worker dependency, NO key->ref/key->slot/authority/token/observation semantics in Rust. LIVENESS INVARIANT (slice 4, `actor::drain_jobs`): the single `spawn_local(drive())` DriveFuture owns spawned capability/timer futures but is woken ONLY by `spawner.push`/a spawned-task waker, so the command loop must ALSO (a) drain BARE QuickJS jobs (enqueued by sync host callbacks like `fire_due_timers`/push, which wake nobody) via `Ctx::execute_pending_job` — NEVER `AsyncRuntime::execute_pending_job`, which polls the spawned-task scheduler and steals its SINGLE waker slot (`schedular.rs:145`, last-poller-wins) — and (b) kick the DriveFuture via a no-op `ctx.spawn(async {})` after each command, because a command's `WithFuture` (eval_async/push) steals that slot. Regression proofs: `js_env_shell.rs` (timer-fired promise continuation), `js_env_liveness.rs` (capability resolution after slot steal). IN-FLIGHT TIMER INVARIANT (Bead 9sp): `EvalAsync` and `Push` each remain the sole active `OwnerCommand` while an OUTER driver awaits their one pinned `AsyncContext::async_with` future and concurrently services the authoritative timer registry through notifications and exact next-due deadlines. It admits no later command and introduces no fixed tick, but globally due guest timers may re-enter JavaScript while that command is suspended; each timer wake uses the same `fire_due_timers` -> `Ctx::execute_pending_job` drain -> no-op-spawn scheduler handoff before the command is polled again. Dropping a caller wait does not cancel an already queued command, and finite work drains before a queued Shutdown. SHUTDOWN POLICY (Bead b41): shutdown is a BOUNDED WHOLE-DRAIN, never a cancel of finite work — `shutdown()` uses `DEFAULT_SHUTDOWN_GRACE` (5 s), `shutdown_within(grace)` makes the bound explicit, and both return `ShutdownOutcome {Clean, AbandonedInFlight, OwnerGone}` (`OwnerGone` = the owner thread exited without answering; nothing is claimed about queued commands). A later request only ever TIGHTENS a recorded deadline. ONE absolute deadline is recorded before the `Shutdown` command is enqueued and checked at the TOP of every in-flight pump iteration (never as a trailing `select!` branch, which a continuously-ready 1 ms timer branch would starve); the pump's sleep is clamped to it and introduces no tick while the deadline is not due. Past the deadline the in-flight `EvalAsync`/`Push` (and every later one dequeued before `Shutdown`) is ABANDONED: its pinned `async_with` future is dropped and its caller receives an EXPLICIT error naming the command kind — including the std-channel `push_blocking` producer thread, which unblocks — never a silent drop; commands still queued when the loop exits get `owner dropped response` as before. Abandonment abandons the OBSERVATION, not the guest computation (the post-command `drain_jobs` + no-op-spawn kick still runs and repairs the scheduler waker slot). The off-lock `JS_FreeValue` of the dropped future is sound ONLY because of the dedicated-owner-thread architecture (documented in `actor.rs`). NOT covered by the bound: ANY non-returning synchronous JavaScript whichever command carries it (a sync `Eval`/`WithContext`, or a `while(true){}` inside an `EvalAsync`/`Push` body — the pump cannot preempt one poll; needs a QuickJS interrupt handler, Bead fls), a sync command dequeued after the deadline, and a self-perpetuating job storm inside `drain_jobs` (Bead c4g). Dropping the actor WITHOUT `shutdown*` is NOT a full shutdown: `Drop` only records the default deadline and enqueues `Shutdown` non-blocking (no join), so a leaked actor's owner thread self-terminates. Proofs: `js_env_liveness.rs` (never-settling eval; never-settling push unblocking its producer thread; abandonment with a live spawned capability future in the scheduler; long-grace negative control with the deadline LIVE during the window: finite work drains Clean and the guest fire hook is not called; dropping the actor still bounds an in-flight command). This is a TEMPORARY bounded fallback substrate (JS WASM Component/WIT is the preferred architecture, upstream-blocked on async guest imports — ComponentizeJS #335); it has an explicit deletion criterion and is NOT a precedent for per-language native bridges. |
| Environment <-> Images TEST capability (3zb-A only) | `hosts/linux` test-only deterministic Images capability | current (slice 3B) | Sole owner of the SCRIPTED Images-capability outcomes used to exercise the Environment during 3zb-A (opaque fake object records/tokens, scripted observation/error outcomes) behind the same plain-data Images port shape. It implements the PORT's observable contract only: opaque-token IDENTITY comparison to dispatch a scripted conflict (NOT a CAS model the guest could reason about), and the observation LANE's pull contract (live-follow-from-high-water default vs incremental cursor) the port must speak. It MUST NOT reproduce substrate INTERNALS: no CAS model, no authority calculus, no revision/cursor STORAGE semantics, no Project storage, no codec semantics — those remain lagrange-images-owned and are complemented by the independent real portable Images composition in 3zb-B; the fake remains the plain-data boundary proof (substrate prerequisite lagrange-images-yss). Test-only: the production embedding must not reference it or the real `lagrange_images` substrate crate (structural-guard enforced). |
| In-process Environment/GTK acceptance protocol (3zb-B3) | `hosts/linux/tests/support/in_process_acceptance.rs` | current (test-only shared oracle, Parts 2A/2B) | Sole owner of the common TEST protocol from `__session.open(rootObjectId)` through NAV/OBS/OLM/STALE/DENIED/UNAVAILABLE/C1 and awaited teardown: GTK intent extraction, renderer-host pumping, causal phase ordering, exact guest-report schemas, flavor expectations and assertions. Fake and real guest compositions own their own bootstrap/dependency injection and implement the pinned `__session` seam; they do not own or copy the common oracle. The protocol body consumes only `JsEnvActor` and `RendererPortHost`; its test bootstrap helpers initialize GTK and provide the deliberately unexercised GLB stub. It implements no Environment, Images, authority, token/cursor, renderer-operation, descriptor-decoding or GTK-realization semantics. Production ownership remains with `ImageClientAdapter`, `hosts/linux/src/js_env/`, `LinuxRendererAdapter` and `semantic_gtk`; fake Images outcomes remain with the preceding row. Primary proofs are `tests/js_env_acceptance.rs` (scripted capability boundary) and `tests/real_images_acceptance.rs` (pinned real Images full loop). |
| Images portable-runtime artifact -> embedded rquickjs modules | `hosts/linux/src/images_composition/portable_artifact.rs` | current (Bead stx) | Sole owner of translating the exact pinned `lagrange-images-portable-runtime/v1` bytes into rquickjs module resolution/loading. Images owns artifact production, canonical validation, closure definition and published content identity; this interaction owner verifies the reviewed raw identity, reads only the supported format/entry/modules fields, preserves exact `src/.../*.js` module identity, normalizes relative imports without allowing escape above `src/`, supplies exact overlays only in the disjoint `host/...` namespace, and exposes only the explicit `portable-runtime` alias for the public entry. It has NO filesystem/sibling/package/Node fallback and does NOT reconstruct Images' artifact validator or closure walker. Primary proofs: `portable_images_artifact.rs` (raw-byte pin, closed resolver namespace, same identity through alias/canonical entry, full closure link) and `host_runtime_structural_guard.rs` (production no-fallback/non-vacuity fence). |
| Env <-> Images crypto BINDING (3zb-B1b) | `hosts/linux/src/images_composition/` (guest bootstrap `crypto-bootstrap.js`) | current (Bead 3zb slice B1b) | Sole owner of BINDING the generic native crypto primitives to the `lagrange-images` crypto-provider contract: it adapts the host's positional primitives to the contract's object form, and installs them through the PUBLIC `setDefaultCryptoProvider` seam re-exported by `portable-runtime`. It is COMPOSITION code, so — unlike `hosts/linux/src/js_env/` — it MAY import public portable Images modules; the sibling-directory boundary is the ownership statement and the structural guard checks both sides of it. INVARIANTS: it implements NO cryptography (no `Math.random`, no `createHash`/`createCipheriv`, no `crypto.subtle`); it does NOT reproduce `assertCryptoProvider` (installing through the public seam IS the validation, since `setDefaultCryptoProvider` runs it and returns the frozen provider); it does NOT deep-import the private `support/default-crypto` module; and it enforces the MANDATORY ordering — `createPortableRuntime` calls `getDefaultCryptoProvider()` at its top to fail fast, so provider installation strictly precedes composition. Proof: `tests/native_crypto_images.rs` drives host primitives -> adapter -> public seam -> real Images consumers (SHA via `typeFingerprint`, AES via a real observation-cursor encode/resume, random via the cursor IV, UUID via the executor's default `cursorSecret`), with two falsifier legs that miswire one operation each and prove the corresponding real Images consumer breaks. |
| Environment <-> portable Images in-process TEST composition (3zb-B3) | B3 real-Images composition (`hosts/linux/tests/support/public_entry_composition_loader.rs`, `real-images-composition.mjs`, and `real-images-acceptance-composition.mjs`) | current (headless adapter chain + full Environment/GTK loop) | Sole owner of TEST-ONLY source loading, bootstrap and dependency injection between the unchanged Environment modules and pinned real Images portable runtime inside rquickjs. The shared test loader dispatches artifact resolution before the flat Environment loader, rejects every external direct `src/...` request, and audits the nonempty public-entry crossing; guest bootstrap installs the reviewed crypto binding before `createPortableRuntime`, injects only helpers from `portable-runtime`, provisions schema/data/guest-only authorities, and closes the runtime backend at teardown. It owns no Environment, Images, renderer or acceptance-protocol semantics: those remain with their production owners and the shared oracle row above; artifact byte/module translation remains with `portable_artifact.rs`. It has no sibling/filesystem/package/Node fallback and does NOT prove the plain-data/WIT-shaped Images port. Primary proofs: `tests/real_images_semantics.rs` (narrow headless adapter semantics plus source/public-entry audit) and `tests/real_images_acceptance.rs` (real authority/CAS/observation through the full Environment and LinuxRendererAdapter/GTK, with opaque tokens guest-only). |

## Ownership change protocol

Before implementing a new major subsystem or interaction:

1. identify the responsibility in the plan/Bead
2. name exactly one owner here
3. identify which existing owner loses or delegates responsibility, if any
4. state the boundary contract/invariants
5. identify the primary proof/tests
6. only then implement

A proposal that says responsibility is "shared", "co-owned", "handled on both sides", or leaves the interaction owner implicit is incomplete.

## Detecting ownership drift

Treat these as defects:

- two modules persist competing versions of the same semantic state
- two endpoints both retry/reconcile the same interaction independently
- validation/business rules are copied to both sides of a boundary without one authoritative source
- a UI adapter starts deciding image semantics
- a lower substrate learns presentation/session policy
- two registries select implementations for the same extension point
- an integration test cannot say which component is responsible for a failure

When drift is found, do not merely synchronize the duplicate implementations. Restore one owner and make the other side consume it.
